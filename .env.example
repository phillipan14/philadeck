# Optional: Unsplash API key for image search (falls back to placeholders without it)
UNSPLASH_ACCESS_KEY=

# AI API keys are entered by users in the app settings (stored in localStorage)
# No server-side AI keys needed
